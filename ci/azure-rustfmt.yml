jobs:
  # Check formatting
  - job: run_rustfmt
    displayName: Check rustfmt
    pool:
      vmImage: $(vmImage)
    steps:
      - script: |
          cargo fmt --all -- --check
        displayName: Check formatting